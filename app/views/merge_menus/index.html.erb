<h1>menu</h1>


<%= form_tag("/fetch_menu") do -%>
	seleccione el restaurante:
	<%= select_tag 'merge_restaurant_id', options_for_select(@merge_restaurant.collect{ |u| [u.connection.name, u.id] }), include_blank: true %>
	<br>

  <div><%= submit_tag 'consultar' %></div>
 <%= @response.to_json %>

menu gloria
<%= select_tag 'proveedor_id[]', options_for_select(@merge_restaurant.collect{ |u| [u.connection.name, u.id] }), include_blank: true %>
<%= select_tag 'proveedor_id[]', options_for_select(@merge_restaurant.collect{ |u| [u.connection.name, u.id] }), include_blank: true %>
<%= select_tag 'proveedor_id[]', options_for_select(@merge_restaurant.collect{ |u| [u.connection.name, u.id] }), include_blank: true %>

<% end -%>

